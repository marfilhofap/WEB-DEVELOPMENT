<?php 

// 1. basic syntax :


// 2. Comment

    /*
    - //
    - /***
    */


//  3. Standar Output

    /*
    - echo, print
    - var_dump
    - "" and ''
    */


// 4. Writing :

    /*
    - PHP in HTML
    - HTML in PHP
    */


// 5. Variavel

    /*
    - case-sensitive : $myVar != $myvar
    - Variable names must start with a letter or an underscore (_).
    - After the initial letter, variable names can contain letters, numbers, and underscores.
    - Variable names cannot contain spaces or special characters, except for underscores.
    - Variable names should be descriptive and meaningful, so they are easy to understand and maintain.
    
    Ex:
        $nome = 'Marfilho Fidel Alvares Pereira';
        $date_of_birth = '1997-09-03';
        $addre55 = 'Bidau Santana';

        interpolation : echo "Ola, hau nia naran $nome";
    */

// 6. Operator :  

    /*
    - Arithmetic (+ - x / %),
    - Concatenation (.)
    - Assignment (=, +=, -=, x=, /=, %=, .=),
    - Comparison (<, >, <=, >=, ==, !=),
    - Identity (===, !==),
    - logic (&&, ||, !)

    Ex:
        $x = 10; $y = 20; 
        echo $x * $y;

        $naran = "Marfilho";
        $apelido = "Pereira"
        echo $naran . " " . $apelido;

        $x = 1; 
        $x += 5;
        echo $x; 

        var_dump(5 < 2); var_dump(1 == "1");
        
        var_dump(1 === "1");

        $x = 10;
        var_dump($x < 10 && $x % 2 == 0);

    */


?>
