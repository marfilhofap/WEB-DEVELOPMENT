<?php
// 1. Standart Output

// echo, print
// var_dump("Fidel");


// 2. Writing

// - PHP IN HTML

// - HTML IN PHP

// 3. Variable

$naranKompletu = "Fidel Pereira";
$Naran = "Sarra";

// echo $Naran;

// 6. Operator :  

    //- Arithmetic (+ - x / %),

    // echo 11 % 2;

    // - Concatenation (.)

    $naran = "Marfilho";
    $apelido = "Pereira";
    // echo $naran . " " . $apelido;

    // - Assignment (=, +=, -=, x=, /=, %=, .=)

    $x = "10";
    // $x += 5;
    // $x -= 7;
    
    // echo $x;

    // Comparison (<, >, <=, >=, ==, !=),

    // var_dump($x == "10");

    // - Identity (===, !==),

    // var_dump($x !== "10");

    // - logic (&&, ||, !)

    /*   
    T && F == F
    F && T == F
    F && F == F
    T && T == T 
    */

    $x = 10;
    var_dump($x <= 10 && $x % 2 == 0);

     /*   
    T || F == T
    F || T == T
    T || T == T 
    F || F == F
    */


?>


